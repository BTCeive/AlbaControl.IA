package com.albacontrol.ml

import android.content.Context

object TesseractOcrProcessor {
    // Minimal stub: attempts to initialize tessdata in a real app; here return false conservatively
    fun init(context: Context): Boolean {
        return false
    }
}
